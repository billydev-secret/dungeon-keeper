-r requirements.txt
ruff>=0.15.22
mypy>=2.3.0
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
pytest-xdist>=3.8.0
freezegun>=1.5.5
hypothesis>=6.157.2
syrupy>=5.5.3
httpx>=0.28.1
pre-commit>=4.6.0
invoke>=3.0.3
aiosqlite>=0.22.1
pyright>=1.1.411
PyYAML>=6.0.3
# Browser-driven responsive-layout gate (tests/web/test_mobile_layout.py). The
# pip package is small; the Chromium build is installed separately
# (`python -m playwright install chromium`) only where the sweep runs — nightly
# CI and dev machines. The default suite and per-push CI skip these tests.
playwright>=1.61
