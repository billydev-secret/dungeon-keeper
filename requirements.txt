discord.py>=2.7.1
python-dotenv>=1.2.2  # NOTE: do NOT install the separate `dotenv` PyPI package (v0.9.9) — it is a deprecated wrapper around python-dotenv and must not be used
anthropic>=0.117.0
fastapi>=0.139.2
python-multipart>=0.0.32
uvicorn[standard]>=0.51.0
pydantic>=2.13.4
httpx>=0.28.1
itsdangerous>=2.2.0
jinja2>=3.1.6
tzdata>=2026.3
psutil>=7.2.2
vaderSentiment>=3.3.2
wavelink>=3.5.2
spotipy>=2.26.0
pynacl>=1.6.2
nudenet
mediapipe
Pillow
pilmoji
numpy
matplotlib>=3.11.1
# llama-cpp-python lives in requirements-local-llm.txt — it is the only
# dependency here that builds from source (no manylinux wheel for cp314), so
# hosts using the remote llama-server backend skip a full C++ toolchain by
# not installing it. See deploy/README.md.
huggingface_hub>=1.24.0
faster-whisper>=1.2.1
