default_stages: [pre-commit]

repos:
  - repo: local
    hooks:
      - id: lint
        name: lint (ruff + pyright)
        entry: ./.venv/Scripts/python.exe -m pytest -q tests/test_lint.py
        language: system
        pass_filenames: false
        always_run: true
