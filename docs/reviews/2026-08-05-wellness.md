# Wellness — battery review, 2026-08-05

Bundle: `wellness_cog.py` (577), `wellness_{service,scheduler,enforcement,
partners,ai}.py` (3.4k), `web_server/wellness_routes/{api,admin,deps}.py`
(1.4k), 7 panels. Spec: `wellness_guardian_spec.md` (flagged in INDEX —
provisioning gap documented). Tests: 3 logic-layer files.

## Architecture — clean bill

- Correct layering throughout; the member/admin route split
  (`wellness_routes/api.py` vs `admin.py`) is the right shape: member routes
  derive identity from the session (`user.user_id`), never from a parameter
  — IDOR-immune by construction; spot-checked the one param-taking route
  (`DELETE /partners/{partner_id}`) and it checks guild + participant
  (api.py:1010-1013) ✓. Admin routes gate on `require_manage_server` ✓.
- **Retention sweeps actually exist and are wired**: `gc_opted_out_users`
  (30-day post-optout settings purge, scheduler.py:163) and
  `gc_old_cap_data`. This is the only feature so far with working TTLs —
  cite as house pattern in synthesis.
- AI nudge copy runs through local Ollama only (`wellness_ai.py:42`);
  degrades to canned text when unavailable. No external processor.

## UX / Docs — minor

- Consent model is the strongest in the bot: explicit opt-in w/ timezone,
  `public_commitment` a separate later choice, opt-out lifts slow-mode and
  blackout markers immediately, paused users skipped in digests.
- D1: spec stays INDEX-flagged for the provisioning gap (new guild needs a
  hand-seeded `wellness_config` row) — still true (guild B row: role 0 /
  channel 0, dormant). Fine as documented; a dashboard "activate wellness"
  admin action would close it, but that's roadmap, not drift.

## GDPR — good; two notes

- All wellness tables covered by `purge_user_data` ✓ (privacy-core), plus
  self-service opt-out + 30d GC. Register rows already marked YES.
- G1: this is health-adjacent data (usage-control regimes, streaks,
  "clean streak" framing, crisis_resource_url). Sensitivity is high even
  though contents are behavioral. Admin panel (`admin_users_data`) lists
  participants to any manage-server holder — defensible (they run the
  program) but worth one line in the spec's privacy section naming who can
  see the participant list.
- G2: partner-request DM (api.py:961-982) sends requester name to the
  target — fine (consent flow), and dissolution is party-checked.

## Loose-ends §8 — RECONCILED

Prod `wellness_config` rows exist for both guilds and match what shipped
code reads (main guild live: role/channel/list-message seeded, enforcement
'gradual'; guild B dormant with 'gentle'). The 07-30 "hand-edited ahead of
merge" memory concern is stale — no drift between prod rows and the shipped
schema. Closed.

## Verdict

No priority findings. Wellness + Whisper are the two model citizens; the
synthesis should hold other features to their patterns (consent ceremony,
self-service erasure, wired GC).
